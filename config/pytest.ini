[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = -q --showlocals
filterwarnings =
    ignore::DeprecationWarning
markers =
    smoke: quick smoke tests for import/availability
    unit: unit tests
norecursedirs = tests/archive
